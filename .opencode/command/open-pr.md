---
description: Push the current branch and open a PR — never merges it
---
Run `./scripts/open-pr.sh` with a title and body summarizing this task's
commit. After opening the PR, stop — merging is the maintainer's decision
only (AGENTS.md §7.5). Do not run `gh pr merge` under any circumstance.
