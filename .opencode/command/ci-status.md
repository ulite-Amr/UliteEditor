---
description: Compact CI status for the current PR — no raw logs
---
Run `./scripts/ci-status.sh` and summarize which checks passed/failed. If any
failed, offer to run `./scripts/err-compact.sh` or `./scripts/test-summary.sh`
next rather than fetching full CI logs.
