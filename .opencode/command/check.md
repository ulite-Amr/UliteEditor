---
description: Run all allowed local checks (fmt, clippy, check, tests) — compact output only
---
Run `./scripts/local-check.sh` and report the result. Do not run a release/full
build separately — this script covers everything permitted locally per
AGENTS.md §2.
