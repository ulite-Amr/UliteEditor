package com.uliteeditor.editor.theme

import androidx.compose.material3.Typography

/**
 * Shared type ramp for the editor chrome. Body glyph styles render on the
 * screen; the M3 Expressive token set (pending vendor) will replace the
 * defaults here and is deliberately not guessed at in this scaffold.
 */
internal val AppTypography = Typography()